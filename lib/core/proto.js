module.exports = (function () {
    'use strict';

    var bigine = {
        core: {}
    };

    bigine.core.proto = function() {
    };

    bigine.core.proto.extends = function(parent, trait) {
    };

    return bigine.core.proto;
})();
